<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Contacts | <%= @crm_app_name %></title>
</head>
<body>
  <h1><%= @crm_app_name %></h1>
  <h2>Contacts</h2>

  <ul>
  <% $rolodex.contacts.each do |contact| %>
    <li><%= contact.first_name %> <%= contact.last_name %></li>
  <% end %>
</ul>
</body>
</html>

